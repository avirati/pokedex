angular.module('app')
.controller('ctrl.main', [
  '$scope',

  function ($scope) {
    //Variables
    angular.extend($scope, {});

    //Functions
    angular.extend($scope, {
      init: function () {

      }
    });

    $scope.init()
  }]);
