angular.module('app.controllers')
.controller('ctrl.pokedex', [
  "$scope",

  function ($scope) {

    //Variables
    angular.extend($scope, {});

    //Functions
    angular.extend($scope, {
      init: function () {

      }
    })

    $scope.init()
  }])
